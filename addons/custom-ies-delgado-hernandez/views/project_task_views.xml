<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Esta vista hereda de la vista de formulario de tareas del módulo project -->
    <record id="view_task_form_hide_partner_id" model="ir.ui.view">
        <field name="name">project.task.form.inherit.hide.partner</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.view_task_form2"/>
        <field name="arch" type="xml">
            <field name="partner_id" position="attributes">
                <!-- Oculta el campo partner_id en el formulario de tareas -->
                <attribute name="invisible">1</attribute>
            </field>
        </field>
    </record>
</odoo>